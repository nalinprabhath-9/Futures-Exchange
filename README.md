# Futures-Exchange

docker compose up oracle --build

Example:

curl -s localhost:8080/price/BTC | python3 -m json.tool

curl -s localhost:8080/price/ETH | python3 -m json.tool

<http://localhost:8080/docs>
